# Generated by Chef

# The name of the CloudFiles bucket in which to store backups.
# Should be one per backup set.
CLOUDFILES_BUCKET="<%= @bucket %>"

REPOSITORY_URL="cf+http://$CLOUDFILES_BUCKET"

FULL_BACKUPS_TO_KEEP="<%= @full_backups_to_keep %>"

. /etc/duplicity/keys.sh
